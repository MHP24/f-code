import { FC } from 'react';

interface Props {
  _id: string;
  slug: string;
  difficulty: number;
  language: string;
}

export const AdminChallengeCard: FC<Props> = ({ _id, slug, difficulty, language }) => {
  return (
    <div>
      
    </div>
  )
}
